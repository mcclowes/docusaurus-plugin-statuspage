{
  "name": "statuspage-example-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "docusaurus start",
    "build": "npm run build:plugin && docusaurus build",
    "build:plugin": "cd ../.. && npm install --legacy-peer-deps && npm run build",
    "serve": "docusaurus serve",
    "clear": "docusaurus clear"
  },
  "dependencies": {
    "@docusaurus/core": "^3.10.1",
    "@docusaurus/preset-classic": "^3.10.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "engines": {
    "node": ">=18"
  }
}
